<div class="page-header">
<h1>Alle Posts</h1>
</div>

<div class="page-header">
  <h2><%= link_to "Nieuwe post", new_post_path, class:"btn btn-success" %></h2>
</div>

<% @posts.each do |post| %>
<div class="status">
  <strong><%= post.user.full_name %></strong>
  <p><%= post.content %></p>
  <div class="meta">
   <%= link_to time_ago_in_words(post.created_at) + " ago", post %>
    <span class="admin">
       <%= link_to raw('<i class="icon-edit"></i>'), edit_post_path(post) %></a>
       <%= link_to raw('<i class="icon-trash"></i>'), post, method: :delete, data: { confirm: "Weet je het zeker?"} %>
    </span>
  </div>
</div>

<% end %>